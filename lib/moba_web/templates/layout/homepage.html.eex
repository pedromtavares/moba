<!DOCTYPE html>
<html lang="en">
    <%=render "_head.html", conn: @conn, current_hero: nil%>
    <style>
        * {
          margin: 0;
          padding: 0;
        }

        @font-face {
          font-family: pressStart;
          src: url("/fonts/start.ttf");
          font-display: swap;
        }

        html,
        body {
          width: 100%;
          height: 100%;
          padding-bottom: 0;
        }

        .hero {
          width: 100%;
          height: 100%;
          min-width: 100%;
          min-height: 100%;
          position: relative;
        }
        .hero::before {
          background-image: url(/images/home.jpg);
          background-size: cover;
          content: "";
          display: block;
          position: absolute;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          z-index: -2;
          opacity: 0.7;
        }
        .sub-hero{
          background-image: url(/images/home.jpg);
          background-position: center;
          max-width: 1350px;
          width: 100%;
          min-height: 730px;
          box-shadow: 0px 0px 50px 50px rgba(0,0,0,1);
        }
        .flex-center {
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-content: center;
        }
        .start{
          background-color: #d62d45;
          border-color: #d62d45;
          box-shadow: 0px 0px 12px 6px rgba(0,0,0,0.75);
          font-weight: bold;
          font-family: pressStart;
          font-size: 0.8rem;
        }
    </style>
    <body style="background: black">
        <div class="hero flex-center">
            <div class="sub-hero flex-center">
              <a href="/start" class="btn btn-lg btn-danger start">PRESS START</a>
            </div>
        </div>
        <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    </body>
</html>